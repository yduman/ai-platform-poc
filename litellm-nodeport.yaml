apiVersion: v1
kind: Service
metadata:
  name: litellm-nodeport
  namespace: ai-platform
spec:
  selector:
    app: litellm
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30400
  type: NodePort
